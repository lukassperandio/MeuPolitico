import { ExpenseCategoryLabelPipe } from './expense-category-label-pipe';

describe('ExpenseCategoryLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new ExpenseCategoryLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
